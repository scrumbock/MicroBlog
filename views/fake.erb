<!-- my_user = User.last

my_user.update(username: "kenzo15")

find a specific user
user.find or user.last

my_user.destroy -->


<!-- User.find(session[:user_id]).for each do |key, value|
  key  << value
end -->
